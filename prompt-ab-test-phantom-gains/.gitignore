# Raw response bodies, one file per request, regenerated by scripts/run.py.
# A rerun produces different bodies anyway, which is the article's finding.
# The ten responses the article quotes are in quoted-examples.json.
transcripts/
